import React, { useState, useEffect } from 'react';\nimport { Globe } from 'lucide-react';\nimport '../styles/components/DigitalClock.css';\n\nfunction DigitalClock() {\n  const [times, setTimes] = useState({});\n\n  const timeZones = [\n    { name: 'Kuala Lumpur', timezone: 'Asia/Kuala_Lumpur', flag: '🇲🇾' },\n    { name: 'Paris', timezone: 'Europe/Paris', flag: '🇫🇷' },\n    { name: 'New York', timezone: 'America/New_York', flag: '🇺🇸' },\n    { name: 'Tokyo', timezone: 'Asia/Tokyo', flag: '🇯🇵' },\n    { name: 'Sydney', timezone: 'Australia/Sydney', flag: '🇦🇺' },\n    { name: 'Dubai', timezone: 'Asia/Dubai', flag: '🇦🇪' }\n  ];\n\n  useEffect(() => {\n    const updateTime = () => {\n      const newTimes = {};\n      timeZones.forEach((tz) => {\n        const now = new Date();\n        const utcTime = now.toLocaleString('en-US', { timeZone: 'UTC' });\n        const tzTime = now.toLocaleString('en-US', { timeZone: tz.timezone });\n        const [date, time] = tzTime.split(', ');\n        const [timeOnly] = time.split(' ');\n        const [hours, minutes, seconds] = timeOnly.split(':');\n        \n        newTimes[tz.timezone] = {\n          time: `${String(hours).padStart(2, '0')}:${String(minutes).padStart(2, '0')}:${String(seconds).padStart(2, '0')}`,\n          date: date,\n          name: tz.name,\n          flag: tz.flag\n        };\n      });\n      setTimes(newTimes);\n    };\n\n    updateTime();\n    const interval = setInterval(updateTime, 1000);\n\n    return () => clearInterval(interval);\n  }, []);\n\n  return (\n    <section className=\"digital-clock-section\">\n      <div className=\"clock-header\">\n        <Globe size={28} />\n        <h2>Horloge Mondiale</h2>\n      </div>\n      <p className=\"clock-subtitle\">Heure actuelle dans différentes zones horaires</p>\n      \n      <div className=\"clocks-grid\">\n        {timeZones.map((tz) => (\n          <div key={tz.timezone} className=\"clock-card\">\n            <div className=\"clock-card-header\">\n              <span className=\"timezone-flag\">{tz.flag}</span>\n              <span className=\"timezone-name\">{tz.name}</span>\n            </div>\n            <div className=\"clock-display\">\n              {times[tz.timezone]?.time || '--:--:--'}\n            </div>\n            <div className=\"clock-date\">\n              {times[tz.timezone]?.date || '-- -- --'}\n            </div>\n          </div>\n        ))}\n      </div>\n    </section>\n  );\n}\n\nexport default DigitalClock;\n", "path": "src/components/DigitalClock.js"}